{
    "config": {
      "step": {
        "user": {
          "data": {
            "idrac_ip": "iDRAC IP Address",
            "idrac_username": "iDRAC Username",
            "idrac_password": "iDRAC Password",
            "mqtt_server": "MQTT Server Address",
            "mqtt_port": "MQTT Port",
            "mqtt_username": "MQTT Username",
            "mqtt_password": "MQTT Password"
          },
          "title": "Dell Server Power Monitor - Setup"
        }
      }
    },
    "options": {
      "step": {
        "init": {
          "data": {
            "energy_price": "Energy Cost per kWh (in currency unit)"
          },
          "title": "Power Consumption Settings"
        }
      }
    },
    "services": {
      "get_power_data": {
        "description": "Fetch the current power usage from iDRAC.",
        "name": "Get Power Data from iDRAC"
      },
      "publish_power_to_mqtt": {
        "description": "Manually publish the latest power data to MQTT.",
        "name": "Publish Power Data to MQTT"
      }
    },
    "state": {
      "sensor": {
        "dell_server_power": {
          "name": "Dell Server Power Consumption",
          "state": "{state} W"
        }
      }
    }
  }